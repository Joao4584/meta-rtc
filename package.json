{
  "name": "meta-webrtc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@9.8.1",
  "scripts": {
    "dev": "husky run pre-dev && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "turbo": "^2.1.3"
  },
  "dependencies": {
  }
}
